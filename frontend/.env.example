# Base URL of the deployed backend, e.g. https://api.example.com -- leave empty for local dev
# (uses the Vite proxy) or any deploy where frontend and backend share one origin.
VITE_API_BASE_URL=

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
